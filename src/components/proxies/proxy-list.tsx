import { List, ListItem } from "@mui/material";

interface Props {
  nodes: IProxyItem[]
}

const ProxyList = (props: Props) => {
  return (
    <List>
      <ListItem>
        233
      </ListItem>
    </List>
  )
}

export default ProxyList;