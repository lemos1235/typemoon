import { BasePage } from "@/components/base";

const RulesPage = () => {
  return (
    <BasePage full contentStyle={{ height: "100%" }} >
      hello
    </BasePage>
  );
}

export default RulesPage;
